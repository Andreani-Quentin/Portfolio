{% extends 'base.html.twig' %}

{% block title %}Edit Competences{% endblock %}

{% block body %}
	<section class="container">
	    <h1 class="my-5">Edition</h1>
	    <a class="btn btn-info mb-5"href="{{ path('home') }}">Retour</a>
	    {{ include('competences/_form.html.twig', {'button_label': 'Update'}) }}
	    <a class="btn btn-primary mb-3"href="{{ path('competences_index') }}">Back to list</a>
	    {{ include('competences/_delete_form.html.twig') }}
    </section>
{% endblock %}
