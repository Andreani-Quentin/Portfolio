{% extends 'base.html.twig' %}

{% block title %}Edit Timeline{% endblock %}

{% block body %}
    <section class="container">
	    <h1 class="my-5">Edition</h1>
	    <a class="btn btn-info mb-5"href="{{ path('home') }}">Retour</a>
	    {{ include('timeline/_form.html.twig', {'button_label': 'Update'}) }}
	    <a class="btn btn-primary mb-3"href="{{ path('timeline_index') }}">Back to list</a>
	    {{ include('timeline/_delete_form.html.twig') }}
    </section>
{% endblock %}
